{
  "private": true,
  "keywords": [
    "turbo"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --ignore=\"packages/ranklab-web/cypress/**/*\"",
    "start": "turbo run start",
    "codegen": "./bin/codegen",
    "test": "NODE_ENV=test turbo run build test --ignore=\"packages/ranklab-web/cypress/**/*\""
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "turbo": "1.2.6"
  },
  "packageManager": "yarn@1.22.18"
}
