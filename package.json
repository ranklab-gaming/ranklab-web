{
  "name": "ranklab",
  "private": true,
  "keywords": [
    "turbo"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --ignore=\"apps/ranklab-web/cypress/**/*\" --force",
    "start": "turbo run start",
    "codegen": "./bin/codegen",
    "test": "NODE_ENV=test turbo run build test --ignore=\"apps/ranklab-web/cypress/**/*\""
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.5.2",
    "prettier": "^2.8.4",
    "turbo": "1.8.3"
  }
}
