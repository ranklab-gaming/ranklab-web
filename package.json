{
  "private": true,
  "scripts": {
    "dev": "yarn workspace @ranklab/web dev",
    "build": "yarn workspace @ranklab/web build",
    "start": "yarn workspace @ranklab/web start",
    "codegen": "java -jar /usr/local/share/swagger-codegen-cli.jar generate -i http://ranklab-api:8000/openapi.json -o ./packages/ranklab-api -l typescript-fetch -c ./swagger.config.json && cp ./codegen/package.json ./packages/ranklab-api/"
  },
  "workspaces": [
    "packages/*"
  ]
}
