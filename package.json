{
  "name": "@ranklab/ranklab",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "sharp": "rm -rf out && mkdir out && sharp -i 'images/**/*.png' -o out -q 80 -f webp resize"
  },
  "dependencies": {
    "@next/env": "13.4.12",
    "next": "13.4.12"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "3.468.0",
    "dotenv": "16.3.1",
    "prettier": "3.1.0",
    "sharp-cli": "4.2.0",
    "turbo": "1.11.0"
  }
}
