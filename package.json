{
  "name": "ranklab",
  "private": true,
  "keywords": [
    "turbo"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build --ignore=\"apps/ranklab-web/cypress/**/*\"",
    "start": "turbo run start",
    "codegen": "./bin/codegen",
    "test": "NODE_ENV=test turbo run build test --ignore=\"apps/ranklab-web/cypress/**/*\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "turbo": "1.8.0"
  }
}
