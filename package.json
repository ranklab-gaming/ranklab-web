{
  "name": "ranklab",
  "private": true,
  "keywords": [
    "turbo"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "turbo-build": "turbo run build --ignore=\"apps/ranklab-web/cypress/**/*\"",
    "build": "next build",
    "start": "turbo run start",
    "codegen": "./bin/codegen",
    "test": "NODE_ENV=test turbo run build test --ignore=\"apps/ranklab-web/cypress/**/*\""
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "turbo": "1.3.1"
  }
}
